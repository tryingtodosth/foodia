node_modules/
.svelte-kit/
build/
build-capacitor/
.env
.env.*
!.env.example

# Capacitor native platform projects — generated by `npx cap add android`/`npx cap add ios`,
# regenerable via `npm run cap:sync`, not hand-edited source. See CLAUDE.md's Capacitor section.
android/
ios/

# Wrangler (Cloudflare) local state/cache
.wrangler/
.cf-auth/

# Claude Code local-only settings (personal permission allowlist, not shared config)
.claude/settings.local.json

# Local Worker env (ADMIN_EMAILS) — the production value is a Cloudflare secret, never committed.
.dev.vars
